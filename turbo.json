{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["HTTPS_PROXY", "BASE_PATH"],
  "tasks": {
    "dev": {
      "persistent": true,
      "cache": false
    },
    "generate": {
      "dependsOn": ["^generate"],
      "inputs": ["node_modules/@scow/grpc-api/**/*.proto", "protos/**/*.proto", "buf.gen.yaml"],
      "outputs": ["generated", "src/generated"]
    },
    "build": {
      "dependsOn": ["^build", "generate"],
      "outputs": [".next/**", "build/**"]
    },
    "@scow/cli#build": {
      "dependsOn": ["^build", "generate"],
      "outputs": ["build/**", "exe/**"]
    },
    "prepareDev": {
      "dependsOn": ["^build"],
      "outputs": ["src/generated", "build"],
      "inputs": ["src/**"]
    },
    "test": {
      "outputs": [],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "test/**/*.ts", "test/**/*.tsx"]
    },
    "@scow/protos#generate": {
      "inputs": ["../../../protos/**/*.proto", "buf.gen.yaml"],
      "outputs": ["generated/**"]
    },
    "@scow/protos#build": {
      "dependsOn": ["generate"],
      "inputs": ["generated/**"],
      "outputs": ["build/**"]
    },
    "@scow/scheduler-adapter-protos#generate": {
      "inputs": ["protos/**/*.proto", "buf.yaml", "buf.gen.yaml"],
      "outputs": ["generated/**"]
    },
    "@scow/scheduler-adapter-protos#build": {
      "dependsOn": ["generate"],
      "inputs": ["generated/**"],
      "outputs": ["build/**"]
    },
    "lint": {
      "inputs": ["**/*.go", "**/go.mod", "**/go.sum", "**/*.proto", "**/*.tsx", "**/*.ts"]
    },
    "@scow/scow-adapters#prepareDev": {
      "inputs": ["buf.gen.yaml", "buf.genCrane.yaml", "buf.genCrane-ai.yaml"],
      "outputs": []
    },
    "@scow/scow-adapters#lint": {
      "dependsOn": ["prepareDev"],
      "inputs": ["**/*.go", "**/go.mod", "**/go.sum", "buf.gen.yaml", "buf.genCrane.yaml", "buf.genCrane-ai.yaml"]
    }
  }
}
